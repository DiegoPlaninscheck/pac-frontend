import React from 'react';
import PropTypes from 'prop-types';
import styles from './PreRegistrationFields.module.css';

const PreRegistrationFields = () => (
  <div className={styles.PreRegistrationFields}>
    PreRegistrationFields Component
  </div>
);

PreRegistrationFields.propTypes = {};

PreRegistrationFields.defaultProps = {};

export default PreRegistrationFields;
